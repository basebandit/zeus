name: Orders Service - Lint & Test

on:
  push:
    branches:
      - main
    paths:
      - 'apps/orders/**'
  pull_request:
    paths:
      - 'apps/orders/**'
    types:
      - opened
      - synchronize
      - reopened

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/orders

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: apps/orders/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Run pre-commit
        uses: pre-commit/action@v3.0.1
        with:
          extra_args: --files apps/orders/**/*.ts apps/orders/**/*.js apps/orders/**/*.json

      - name: Run tests
        run: npm run test

      - name: Test coverage
        run: npm run test:cov

      - name: Upload coverage reports
        uses: codecov/codecov-action@v4
        with:
          files: ./apps/orders/coverage/lcov.info
          flags: orders-service
          fail_ci_if_error: false
